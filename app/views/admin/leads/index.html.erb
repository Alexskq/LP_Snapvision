<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold mb-8 text-[#2F2E41] font-inter">Administration des leads</h1>
  
  <div class="grid md:grid-cols-2 gap-8">
    <!-- Startups -->
    <div>
      <h2 class="text-2xl font-bold mb-4 text-[#F4C95D] font-inter">Startups (<%= @startup_leads.count %>)</h2>
      
      <div class="bg-white shadow-md rounded-lg overflow-hidden">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Email</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date</th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            <% @startup_leads.each do |lead| %>
              <tr>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900"><%= lead.email %></td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500"><%= lead.created_at.strftime("%d/%m/%Y %H:%M") %></td>
              </tr>
            <% end %>
            
            <% if @startup_leads.empty? %>
              <tr>
                <td colspan="2" class="px-6 py-4 whitespace-nowrap text-sm text-gray-500 text-center">Aucune startup enregistrée</td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
    
    <!-- Developers -->
    <div>
      <h2 class="text-2xl font-bold mb-4 text-[#2F2E41] font-inter">Développeurs (<%= @developer_leads.count %>)</h2>
      
      <div class="bg-white shadow-md rounded-lg overflow-hidden">
        <table class="min-w-full divide-y divide-gray-200">
          <thead class="bg-gray-50">
            <tr>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Email</th>
              <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date</th>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            <% @developer_leads.each do |lead| %>
              <tr>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900"><%= lead.email %></td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500"><%= lead.created_at.strftime("%d/%m/%Y %H:%M") %></td>
              </tr>
            <% end %>
            
            <% if @developer_leads.empty? %>
              <tr>
                <td colspan="2" class="px-6 py-4 whitespace-nowrap text-sm text-gray-500 text-center">Aucun développeur enregistré</td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  
  <div class="mt-8">
    <a href="<%= root_path %>" class="btn bg-[#2F2E41] hover:bg-[#2F2E41]/90 text-white border-none font-inter">
      Retour à l'accueil
    </a>
  </div>
</div> 